FROM cs2025/py-aws-base:latest

# Set working directory
WORKDIR /workspaces/codespaces  # Important: Match the target in devcontainer.json

# Create a non-root user (recommended)
# RUN useradd -ms /bin/bash vscode
# USER vscode

# Copy your project files (optional - usually done via the mount)
# COPY . .  # Not usually necessary because of the bind mount

# Set the entrypoint (optional)
# ENTRYPOINT ["/bin/bash"]

# Or run a command
# CMD ["python", "my_script.py"]